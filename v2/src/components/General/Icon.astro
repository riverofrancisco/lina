---
export interface Props {
  icon: string;
}

const { icon } = Astro.props as Props;
const { default: innerHTML } = await import( /* @vite-ignore */ `/media/icons/${icon}.svg?raw`);
---

<Fragment set:html={innerHTML} />
